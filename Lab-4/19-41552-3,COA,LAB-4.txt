1.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV AX,'4'
CMP AX,'0'

JL IF
JMP EXIT

IF:
MOV BX,'-1'
JMP EXIT

EXIT:
MOV AH,4CH
INT 21H

2.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV AL,'4'
CMP AL,'0'

JL IF
JMP THEN

IF:
MOV AH,0FFH
JMP EXIT

THEN:
MOV AH,'0'
JMP EXIT

EXIT:
MOV AH,4CH
INT 21H

3.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV DL,'D'
CMP DL,'A'

JGE IF
JMP EXIT

IF:
CMP DL,'Z'
JLE THEN
JMP EXIT

THEN:
MOV AH,2
INT 21H
JMP EXIT

EXIT:
MOV AH,4CH
INT 21H

4.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV AX,05H
MOV BX,07H
MOV CX,08H
CMP AX,BX

JL IF
JMP EXIT

IF:
CMP BX,CX
JL THEN
JG ELSE

THEN:
MOV AX,0
JMP EXIT

ELSE:
MOV BX,0
JMP EXIT

EXIT:
MOV AH,4CH
INT 21H

5.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV AX,'9'
MOV BX,'8'
MOV CX,'5'

CMP AX,BX
JB IF

CMP BX,CX
JB IF

MOV DX,'1'
JMP EXIT

IF:
MOV DX,'0'
JMP EXIT

EXIT:
MOV AH,4CH
INT 21H

6.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV AX,'9'
MOV BX,'8'
MOV CX,'5'

CMP AX,BX
JB IF

CMP BX,CX
JB THEN

MOV CX,'0'
JMP EXIT

IF:
MOV AX,'0'
JMP EXIT  

THEN:
MOV BX,'0'
JMP EXIT

EXIT:
MOV AH,4CH
INT 21H

7.
.MODEL SMALL
.STACK 100H
.DATA
.CODE

MOV AL,'1'
CMP AL,'1'

JE ODD
CMP AL,'3'

JE ODD
CMP AL,'2'

JE EVEN
CMP AL,'4'

JE EVEN  
JMP EXIT

ODD:
MOV AH,2
MOV DL,'o'
INT 21H
JMP EXIT

EVEN:
MOV AH,2
MOV DL,'e' 
INT 21H

EXIT:
MOV AH,4CH
INT 21H